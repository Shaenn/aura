.DS_Store
.thumbs.db
node_modules

# Quasar core related directories
.quasar
/dist
/quasar.config.*.temporary.compiled*

# Cordova related directories and files
/src-cordova/node_modules
/src-cordova/platforms
/src-cordova/plugins
/src-cordova/www

# Capacitor related directories and files
/src-capacitor/www
/src-capacitor/node_modules

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor directories and files
.idea
*.suo
*.ntvs*
*.njsproj
*.sln

# local .env files
.env.local*
.env
server/.env

# Personal, non-versioned data (backups, local overrides)
/.local/

# Per-machine Claude Code settings: permissions, local overrides. Never shared —
# and never protected by anything but this line on a fresh clone.
.claude/settings.local.json

# Idea / scratch notes (not versioned)
idea/

# Working notes, kept out of the repository
llm règles/

# Playwright visual harness (local debugging tool, not committed)
harness/
