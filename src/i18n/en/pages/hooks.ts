import type { MessageSchema } from '../../fr'

const hooks: MessageSchema['pages']['hooks'] = {
  note: 'A hook runs an action (command, prompt, HTTP request…) at a point in the lifecycle (before or after a tool, at startup…). Advanced fields that are not shown (env, args, if…) are preserved as they are.',
  execution: 'Hook execution',
  executionHint: 'Disabled, no hook runs (managed ones aside).',
  execOn: 'Enabled',
  execOff: 'Disabled',
  pluginHooks: 'Plugin hooks',
  pluginNote: 'Provided by installed plugins — read-only, managed by the plugin.',
  pluginOn: 'on',
  pluginOff: 'off',
  userHooks: 'Personal hooks (settings.json)',
  empty: "I don't see any personal hook configured.",
  eventAria: '{event} hooks',
  matcher: 'Matcher',
  matcherPlaceholder: 'e.g. Edit|Write  (empty = all)',
  removeGroup: 'Remove group {n}',
  removeAction: 'Remove action {n}',
  actionType: 'Action type',
  timeout: 'Timeout (s)',
  command: 'Command',
  commandPlaceholder: 'Shell command (receives the JSON event on stdin)',
  prompt: 'Prompt',
  promptPlaceholder: 'Evaluation prompt (LLM)',
  advanced: 'type “{type}” — edit it from the JSON tab in Settings',
  addAction: 'Action',
  addGroup: 'Group',
  addOn: 'Add a hook on',
  event: 'Event',
  add: 'Add',
  readError: "I couldn't read your hooks",
  events: {
    SessionStart: 'When a session starts or resumes.',
    SessionEnd: 'When a session ends.',
    Setup: 'On launch in init/maintenance mode (--init-only, --init/-p).',
    UserPromptSubmit: 'When you send a prompt, before Claude processes it.',
    UserPromptExpansion: 'When a typed command expands into a prompt, before it reaches Claude.',
    PreToolUse: 'Before a tool call runs.',
    PostToolUse: 'After a tool call succeeds.',
    PostToolUseFailure: 'After a tool call fails.',
    PostToolBatch: 'After a batch of parallel tool calls resolves, before the next model call.',
    PermissionRequest: 'When a permission dialog is shown.',
    PermissionDenied: 'When a tool call is denied by the auto-mode classifier.',
    Notification: 'When Claude Code sends a notification.',
    MessageDisplay: 'While the text of an assistant message is being displayed.',
    SubagentStart: 'When a sub-agent starts.',
    SubagentStop: 'When a sub-agent ends.',
    TaskCreated: 'When a task is created (TaskCreate tool).',
    TaskCompleted: 'When a task is marked as completed.',
    Stop: 'When Claude finishes answering.',
    StopFailure: 'When the turn ends on an API error.',
    TeammateIdle: 'When a teammate (agent team) is about to go idle.',
    InstructionsLoaded: 'When a CLAUDE.md or .claude/rules/*.md is loaded into the context.',
    ConfigChange: 'When a configuration file changes during the session.',
    CwdChanged: 'When the working directory changes (e.g. a “cd” run by Claude).',
    DirectoryAdded: 'When a directory joins the workspace (/add-dir).',
    FileChanged: 'When a watched file changes on disk.',
    WorktreeCreate: 'When a worktree is created (--worktree / isolation: worktree).',
    WorktreeRemove: 'When a worktree is removed (end of a session or sub-agent).',
    PreCompact: 'Before the context is compacted.',
    PostCompact: 'After the context is compacted.',
    PreModelSwitch: 'Before a model change — the hook can allow it, deny it, or have it confirmed.',
    PostModelSwitch: 'After a model change.',
    Elicitation: 'When an MCP server asks for user input during a tool call.',
    ElicitationResult: 'After the user answers an MCP elicitation, before it goes back to the server.',
  },
}

export default hooks
